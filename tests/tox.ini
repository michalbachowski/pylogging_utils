[tox]
# do not require setup.py
skipsdist = True
envlist = py27,py31,py33,py35

[testenv]
setenv =
    PYTHONPATH = {toxinidir}

deps=
    -r{toxinidir}/requirements.txt

commands= /usr/bin/env make python
